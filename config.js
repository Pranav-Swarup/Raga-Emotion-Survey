// Fill these in once you've created a free Supabase project (see README.md).
// Until SUPABASE_URL is set, the app runs in local-only mode: responses are
// kept in memory and offered as a CSV download at the end instead of being
// sent to a backend. That's fine for testing the flow before you wire up hosting.

const SUPABASE_URL = "";        // e.g. "https://xxxxxxxx.supabase.co"
const SUPABASE_ANON_KEY = "";   // the "anon public" key from Project Settings > API
